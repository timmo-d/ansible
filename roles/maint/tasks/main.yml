- name: Ensure cifs-utils is installed
  apt:
    name: cifs-utils
    state: present
    update_cache: yes

- name: Create mount point
  file:
    path: /mnt/data
    state: directory
    owner: root
    group: root
    mode: '0755'

- name: Create CIFS credentials file
  copy:
    dest: /etc/samba/creds-tim
    content: |
      username=tim
      password=1212
    owner: root
    group: root
    mode: '0600'

- name: Ensure CIFS entry is present in fstab
  mount:
    path: /mnt/data
    src: //192.168.1.106/data
    fstype: cifs
    opts: credentials=/etc/samba/creds-tim,iocharset=utf8,file_mode=0777,dir_mode=0777,nofail
    state: mounted


- name: Copy file to mounted share
  copy:
    src: /var/ossec/api/configuration/api.yaml
    dest: /mnt/data/api.yaml
    remote_src: yes


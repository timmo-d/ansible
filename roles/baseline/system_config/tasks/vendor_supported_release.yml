---
- name: Check vendor-supported release (informational)
  shell: . /etc/lsb-release && echo "$DISTRIB_DESCRIPTION"
  register: lsb
  changed_when: false

- name: Display release info
  debug:
    msg: "Detected release: {{ lsb.stdout }} — Ensure vendor-supported 24.04 LTS build."

---

- name: Disable legacy and insecure network services (best effort)
  systemd:
    name: "{{ item }}"
    state: stopped
    enabled: no
  loop:
    - telnet
    - rsh
    - rexec
    - vsftpd
  ignore_errors: yes

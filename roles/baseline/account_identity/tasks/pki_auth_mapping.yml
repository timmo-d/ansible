---
        # -------------------------------------------------------------------
        # Ubuntu 24.04 LTS — DISA-STIG | account_identity | pki_auth_mapping.yml
        # Guarded by variable: enable_pki_auth_mapping
        # -------------------------------------------------------------------

- name: Install SSSD and smart card tooling (site-specific config follows)
  apt:
    name: [sssd, sssd-tools, opensc]
    state: present

- name: PKI/SSSD mapping requires realm details (placeholder)
  debug:
    msg: "Provide realm, LDAP/IdM, and cert OID details to render SSSD/Kerberos templates."
